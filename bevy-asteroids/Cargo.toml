[package]
name = "bevy-asteroids"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
rand = "0.8.0"
getrandom = { version = "0.3", features = ["wasm_js"] }
uuid = { version = "1", features = ["js"] }
