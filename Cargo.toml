[workspace]
resolver = "2"
members = ["bevy-asteroids", "bevy-space-rts"]

# Shared profile settings — all member crates inherit these
[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1

[profile.wasm-release]
inherits = "release"
opt-level = "z"
lto = true
