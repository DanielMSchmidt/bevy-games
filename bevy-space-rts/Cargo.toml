[package]
name = "bevy-space-rts"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"
bevy-inspector-egui = { version = "0.36", optional = true }

[features]
dev = ["bevy-inspector-egui"]
